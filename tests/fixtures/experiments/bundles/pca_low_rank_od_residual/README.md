# PCA low-rank OD residual fixture

This approved stdlib-only bundle compares a pooled gravity baseline with a rank-k residual
correction on deterministic synthetic OD flows with known latent factors. It uses fixed seeds,
no network, and no subprocesses. Its output supports only the mapped synthetic claim and does not
imply real-world validation or publication readiness.
