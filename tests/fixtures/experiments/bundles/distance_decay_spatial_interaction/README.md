# Distance-decay spatial interaction fixture

This approved stdlib-only bundle compares pooled and origin-specific distance-decay models on
deterministic synthetic OD flows. It uses fixed seeds, no network, and no subprocesses. Its output
supports only the mapped synthetic claim and does not imply real-world validation or publication
readiness.
